export const SIDEBAR_WIDTH_COLLAPSED_PX = 64;
export const SIDEBAR_WIDTH_EXPANDED_PX = 256;

export const SIDEBAR_WIDTH_COLLAPSED = `${SIDEBAR_WIDTH_COLLAPSED_PX}px`;
export const SIDEBAR_WIDTH_EXPANDED = `${SIDEBAR_WIDTH_EXPANDED_PX}px`;

/** Shared height for AppHeader and sidebar brand row */
export const SHELL_HEADER_HEIGHT_CLASS = "h-14";

/** Matches `Button` `size="sm"` — use for header controls and `UserAvatar` `size="md"`. */
export const SHELL_BUTTON_SIZE_CLASS = "h-9 w-9";

/** Press feedback for header icon controls (e.g. stop, account avatar). */
export const SHELL_ICON_BUTTON_PRESS_CLASS =
  "transition-all duration-150 ease-out active:scale-95";

/** Width of the right panel on `/view/*` meeting pages */
export const MEETING_VIEW_PANEL_WIDTH = "650px";
